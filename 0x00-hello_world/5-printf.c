#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: This program prints a message with proper grammar.
 *
 * Return: 0 on success
 */
int main(void)
{
	/* Print the message with proper grammar using printf */
	printf("with proper grammar, but the outcome is a piece of art,\n");
	/* Return 0 to indicate successful execution */
	return (0);
}


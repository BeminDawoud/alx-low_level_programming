#include "main.h"
#include <string.h>

/**
 * _strlen -  a function that returns the length of a string.
 * @s: An input string
 * Return: Nothing
 */

int _strlen(char *str)
{
	int count = 0;

	while(str)
		count++;
	return (count);
}

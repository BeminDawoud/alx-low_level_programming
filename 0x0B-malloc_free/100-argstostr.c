#include "main.h"

/**
* argstostr - dividing words
* @ac: integer
* @av: string
* Return: string or NULL
*/
char *argstostr(int ac, char **av)
{
	if (ac == 0 || av == NULL)
		return (NULL);
	return (NULL);
}
